/**
 * modules/payments/index.js
 * 
 * Public API for the Payments Module.
 * Everything else inside this folder is private and should not be exported.
 */

export { createPayment, getPayments } from "./services/paymentService";
